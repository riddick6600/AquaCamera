final class CameraGalleryConstants {
  const CameraGalleryConstants._();

  static const availableCamerasTimeout = Duration(seconds: 8);
  static const cameraInitializationTimeout = Duration(seconds: 12);
  static const appResumeTimeout = Duration(seconds: 2);
  static const permissionDialogSettleDelay = Duration(milliseconds: 450);

  static const gridCrossAxisCount = 3;
}
